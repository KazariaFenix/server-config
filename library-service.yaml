spring:
  datasource:
    url: jdbc:postgresql://localhost:5415/library
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: create-drop
  kafka:
    consumer:
      isolation-level: read_committed